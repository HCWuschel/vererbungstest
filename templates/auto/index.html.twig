{% extends 'base.html.twig' %}

{% block title %}Auto index{% endblock %}

{% block body %}
    <h1>Auto index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Age</th>
                <th>Lenkradtyp</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for auto in autos %}
            <tr>
                <td>{{ auto.id }}</td>
                <td>{{ auto.name }}</td>
                <td>{{ auto.age }}</td>
                <td>{{ auto.lenkradtyp }}</td>
                <td>
                    <a href="{{ path('app_auto_show', {'id': auto.id}) }}">show</a>
                    <a href="{{ path('app_auto_edit', {'id': auto.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_auto_new') }}">Create new</a>
    <br>

    -start of drag example-
    <h1>Listenauswertung</h1>

    <form action="{{ path('Listenauswertung')}}" method="post" id="Listenauswertung" name="Listenauswertung">
    <ul id="SortierListe">

    {% for Fahrzeug in Fahrzeugsammlung %}
    <li>
        <div class="PlatzhalterModul">
        {{ Fahrzeug.name }}

            <input type="hidden" name=" {{ Fahrzeug.name }}" value="{{ Fahrzeug.id }}-{{ Fahrzeug.idUmfassendeKlasse }}">

        </div>
    </li>
    {% endfor %}
    </ul>

<button type="submit">Check it out</button>

</form>
<h1>ListenauswertungStatus</h1>
    <form action="{{ path('Listenauswertung')}}" method="post" id="ListenauswertungStatus" name="ListenauswertungStatus">
        <ul id="SortierListe" class="SortierListeStatus">

            {% for Fahrzeug in FahrzeugsammlungFuerStatus %}
                <li>
                    <div class="PlatzhalterModulStatus">
                        {{ Fahrzeug.name }}<br>
                        {{ Fahrzeug.status }}

                        <input type="hidden" name=" {{ Fahrzeug.name }}" value="{{ Fahrzeug.id }}-{{ Fahrzeug.idUmfassendeKlasse }}">

                    </div>
                </li>
            {% endfor %}
        </ul>

        <button type="submit">Check it out</button>

    </form>

<h1>Listenauswertung per Usort</h1>

<form action="{{ path('Listenauswertung')}}" method="post" id="ListenauswertungUsort" name="Listenauswertung">
    <ul id="SortierListe" class="ListenauswertungUsort">

        {% for Fahrzeug in Fahrzeugsammlung %}
            <li>
                <div class="PlatzhalterModul">
                    {{ Fahrzeug.name }}

                    <input type="hidden" name=" {{ Fahrzeug.name }}" value="{{ Fahrzeug.id }}-{{ Fahrzeug.idUmfassendeKlasse }}">

                </div>
            </li>
        {% endfor %}
    </ul>
</form>
    <h1>ListenauswertungStatus per Usort</h1>
    <form action="{{ path('Listenauswertung')}}" method="post" id="ListenauswertungStatusUsort" name="ListenauswertungStatus">
        <ul id="SortierListe" class="ListenauswertungStatusUsort">

            {% for Fahrzeug in FahrzeugsammlungFuerStatus %}
                <li>
                    <div class="PlatzhalterModulStatus">
                        {{ Fahrzeug.name }}<br>
                        {{ Fahrzeug.status }}

                        <input type="hidden" name=" {{ Fahrzeug.name }}" value="{{ Fahrzeug.id }}-{{ Fahrzeug.idUmfassendeKlasse }}">

                    </div>
                </li>
            {% endfor %}
        </ul>

        <button type="submit">Check it out</button>

    </form>


{% endblock %}
